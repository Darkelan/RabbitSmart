<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="de.kumodo.rabbitsmart">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <activity
            android:name="de.kumodo.rabbitsmart.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ObjektDetailActivity"
            android:label="@string/title_activity_objektdetail"
            android:parentActivityName=".MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="de.kumodo.rabbitsmart.MainActivity" />
        </activity>

        <activity
            android:name=".EinstellungenActivity"
            android:label="@string/title_activity_einstellungen"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="de.kumodo.rabbitsmart.MainActivity" />
        </activity>

    </application>


</manifest>
